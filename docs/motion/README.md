# Hush Motion System — Phase 1 + Pilot (4 movement families)

**Status 2026-07-07:** bench benchmark + the three-movement pilot (Barbell Back Squat, Barbell Row,
Lat Pulldown) are built and green, and the figure has been through the **embodiment rebuild** —
the Duotone Athlete skin v2 (filled trunk silhouette, tapered limbs, one canonical anthropometry
in `src/motion/anthro.ts`, true-scale ghost plates in `src/motion/kit.ts`) — plus the pre-rollout
**skin v2.1 language pass** (standard §3.1 Amendment 3): the **knockout seam** (near-plane limbs,
head, and fists separate from the body behind them by a thin paper gap, so the working limb can
never dissolve into the trunk) and the **honest-scale ghost dumbbell** (r8 vs the r16 barbell
plate — size hierarchy alone states the implement). Same FormSpecs, same validator; every prior
test passes unmodified. Live loops rendered from the compiled core; committed snapshots below
(`*_demo.gif`, `*_validation.txt`). Interactive review page is generated by
`tools/motion-harness/reviewpage.js`.

**Equipment language (2026-07-07, §3.5 Amendment 4 — recognition first, reduction second):** the
founder inverted the equipment objective — start from the clearest possible representation of the
exercise, simplify second; the machine is the exercise's noun. All seven machine/cable rigs now
draw their complete station from `src/motion/machines.ts`: full weight-stack towers whose
**selected plate visibly rides 1:1 with the handle**, explicit pulley-by-pulley routing on cable
stations (pulldown, seated row, face pull), **frame-fused** selectorized machines under the shroud
license (chest press ram through its yoke, machine-row folding arm), the t-bar shaft in the bar
voice, and face_pull restaged **facing the machine** (camera behind the athlete) so the full cable
column draws honestly. Free-weight rigs untouched — their machine IS the implement.

**Equipment identity (2026-07-07, §3.5 Amendment 5 — one station, one signature):** the founder's
review of the pass moved the bar from *force chain visible* to *each family distinguishable from
silhouette alone with the athlete covered*. Three laws: the stack is the shared voice (never the
identity) · each family owns exactly one signature and no two may share it · **the attachment is
a word** (drawn true, never a generic tick). Fixes: the shoulder press was **de-Smithed** (rails +
sliding carriage read as a Smith machine — now twin press arms fold up from short side pillars;
the gate silhouette is reserved for the real Smith); the chest press grew its **high-back seat**
(pad past the shoulder to head height), splitting the row/press twins; the pulldown bar has its
**downswept tips**, the seated row a **V-grip** with the cable at the apex, the face-pull rope its
**ball-ends**. The covered-athlete test renders via `tools/motion-harness/silhouette.js`. Same
poses, FormSpecs, tempo: 18/18 PASS, jest 566/566. Review page:
`tools/motion-harness/equipmentpage.js`.

**The frontal directive (2026-07-08, §3.4 Amendment 7 — founder decision):** every chest-family
and shoulder-family exercise presents **front-view** — identity lives in upper-body symmetry.
The shoulder family (press_vertical) was already frontal; the whole chest family is restaged:
lying presses (bench benchmark, inclines, dumbbells, close grip) draw from the **head-end
camera** (the spotter's frame — the world-vertical stroke is fully in-plane, canonical arms at
lockout, rack goalpost + end-on bench + both plates stated; `supineFrontCore` in
`src/motion/bodies.ts`, `benchEndOn`/`rackUprights` in `kit.ts`), and the seated machine chest
press draws under the **perspective license** (depth as scale, D=72, true stroke 30 exposed to
the validator as a `stroke` pseudo-joint; fists/handles/struts grow ~1.7× through the emphasized
press; stack rides the 3D stroke 1:1). The committed `bench_press_benchmark.gif` +
`bench_press_validation.txt` are regenerated from the frontal benchmark. 18/18 PASS, jest
567/567 (symmetry now asserted as mirror geometry).


**The full-catalog eye pass (2026-08-25, founder mandate: "take every aspect to the edge"):** all
118 rigs × both athletes × 3 phases reviewed frame-by-frame, and three findings fixed the same day:
· **sumo_deadlift restaged FRONT-VIEW** — the frontal directive extended down: a sumo's identity
  (wide stance, toes out, knees wide, grip inside the knees) is lower-body symmetry, invisible from
  the side; face-on it IS the silhouette, the pull is fully in-plane, and the knees-outside-the-grip
  fact is now a FormSpec predicate, not a caption.
· **smith_calf_raise grew its machine** — it drew a floating plate, no rails, no grip; now the
  Smith's reserved gate (both uprights, crossbar riding the bar) with the bar on the traps at the
  back squat's own carry and grip.
· **leg_press_calf_raise moved onto the SLED** (`legPress.ts`) — it wore the standing machine's
  trap-pad signature on the wrong exercise; now it is the leg press station with legs long and
  pinned, the whole rep a foot pivoting about the ankle, the plate riding the ball of the foot.
· **machine_triceps_ext became a LEVER** (`pushdown.ts`) — it drew the cable members' own
  pulley-and-rope with a grip glyph swapped in, a twin inside the family; now its pivot pin sits
  on the elbow's axis (as the real machines are labeled) and the rigid arm rides the forearm 1:1
  to the fixed grips — the drawn machine sweeps the exact arc the FormSpec validates.
All four validate; the deep pass (every rig × both athletes × 3 phases, judged panel-by-panel on
technique · identity · range · camera · body-and-equipment) found every other rig sound.

**The second athlete (2026-08-25):** the system now has TWO Duotone Athletes on ONE skeleton.
Every segment length in `anthro.ts` is shared — same poses, same FormSpecs, same machines, so every
guarantee above is hers verbatim — and the difference is skin-only by construction: her own side
and frontal trunk profiles (waist cut, full glutes, no lat flare; the frontal shoulder station may
not drop below ~15.8 because the shoulder JOINTS sit at ±15.5 and a narrower line strands the arm
outside the trunk), slimmer tapered limbs (`LIMB_W_F`), a 5.4 neck, and the gym bun — a
silhouette-level word, rigid to the skull, drawn under the head ink with its own knockout seam so
it survives lying, hanging, and hinged poses. `skinFigure`/`buildFrame` take `sex: FigureSex`
defaulting to `'male'`, and `__tests__/motion/figures.test.ts` holds three laws: the default is
him byte-identical; she performs every rig in the registry with finite geometry; her frame differs
from his while the skeleton is exactly shared. In the app, `FormMedia` picks the athlete from
`profile.sex` — the same rule MiniBody follows on Home. Committed snapshots: the four
`*_female_demo.gif` / `bench_press_female_benchmark.gif` beside the originals; the side-by-side
review page is `tools/motion-harness/figures.js`, and `hero.js`/`emit.js` accept a trailing
`male|female` argument.

The exercise demonstration system. A 2D skeletal figure, defined in code, performs each movement
from a `FormSpec` (the canonical definition of the exercise), rendered live in the app. No video
files, no external tools, no hosting — pure TypeScript + `react-native-svg`, validated by CI.

See `docs/canonical/MOTION_FORM_STANDARD_V1.md` for the normative form standard and the full
category model (3 mechanics, 20 templates, all 60 catalog exercises).

## Where it lives

```
src/motion/
  types.ts        FormSpec, Pose, Primitive, Rig — the contracts
  geometry.ts     vectors, two-bone IK, joint-angle measurement, easing
  timeline.ts     canonical tempo → range-of-motion fraction (2 identical reps)
  formspec.ts     validate(rig) — asserts every predicate across the loop (the CI teeth)
  anthro.ts       the canonical athlete: one segment-length table shared by every rig
  kit.ts          shared equipment vocabulary: ghost plates, cables, pulleys, pads, benches
  machines.ts     the complete stations (§3.5 Amendment 4): stack towers, pulldown/row/press rigs
  skin.ts         the Duotone Athlete skin v2 (trunk silhouette + tapered limbs, one renderer)
  palette.ts      token → hex (mirrors src/design/tokens.ts)
  frame.ts        composes scene + equipment + figure into Primitive[]
  library/        one file per exercise: bbBenchPress · bbBackSquat · bbRow · latPulldown
  registry.ts     EXERCISE_MOTION map + exerciseMotion(id)
  render/MotionFigure.tsx   the React Native renderer (consumes the same Primitive[])
```

Two authoring patterns cover all four (and, per the standard, all 60): **Mechanic A** (implement
travels, body anchored — bench, row, pulldown) drives only the bar height and solves the arm by
two-bone IK from the fixed shoulder; **Mechanic B** (the body travels — squat) drives the leg
angles with fixed segment lengths and *solves the torso lean* so the bar stays over the mid-foot,
making the vertical bar path true by geometry rather than a tuned number.

`components/FormMedia.tsx` prefers motion → then the existing video seam → then the static
silhouette, so an exercise lights up the moment its rig is added, with no other change.

## Visual QC (filmstrip / hero PNGs, headless)

`tools/motion-harness/filmstrip.js` and `hero.js` render any rig to a static SVG grid that headless
Chrome screenshots to PNG — the loop used to author and eyeball each movement:

```
npx tsc -p tsconfig.harness.json
node tools/motion-harness/hero.js bb_back_squat out.html "0,0.5,1"
chrome --headless --screenshot=out.png --window-size=1000,760 file://<abs>/out.html
```

## Regenerating the review harness (GIF, filmstrip, live page)

The harness renders the **exact compiled source** the app ships and CI validates — zero duplicated
math. Two steps (PowerShell or bash):

```
# 1. compile the pure motion core to Node-runnable JS
npx tsc -p tsconfig.harness.json

# 2. emit the review surfaces (benchmark.html, gifpage.html, frames.json, validation.txt)
node tools/motion-harness/emit.js <output-dir> bb_bench_press
```

`benchmark.html` is a self-contained review page (live looping figure + filmstrip + validation).
`gifpage.html` encodes an animated GIF in-browser; open it in a browser (or headless with
`--dump-dom`) and pull the `GIFB64:…:END` payload. The committed `bench_press_benchmark.gif` and
`bench_press_validation.txt` here are snapshots of that output.

## The guarantee

`__tests__/motion/bbBenchPress.test.ts` and `__tests__/motion/pilot.test.ts` run the FormSpec
validator on all four rigs on every commit, and also prove the validator has teeth — deliberately
broken rigs (lifted hips, a curved bar path, a partial press, a lifted heel, a swinging torso, a
bar bounced past the collarbone) must fail. The demonstration can never silently contradict Hush's
cues.
