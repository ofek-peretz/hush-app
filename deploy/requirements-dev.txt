# Hush v1 — PINNED test/dev dependencies (BB-31). Used by CI to run the API pytest suite
# (FastAPI TestClient needs httpx). Not installed in the production image.
-r requirements.txt

pytest==9.0.3
iniconfig==2.3.0
pluggy==1.6.0
packaging==26.2

httpx==0.28.1           # FastAPI TestClient transport
httpcore==1.0.9
certifi==2026.5.20
